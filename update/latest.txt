v1.2 RC 4

RELEASE NOTES:

Fixes an issue where SSHRD runs on an already pwned device, which might cause errors.
Adds additional steps beforehand when running SSHRD to back up activation tickets.
This update is strongly recommended for all users.
